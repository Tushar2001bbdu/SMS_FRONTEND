/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},  // Keep this if you're using Tailwind CSS
    autoprefixer: {}, // Keep this if you want vendor prefixes
  },
};

export default config;
